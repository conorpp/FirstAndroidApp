<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phoneapp.app" >



    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"

        >
        <activity
            android:name="com.example.phoneapp.app.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".sensors.SensorPicker"
            android:label="@string/sensor_name" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name=".countdown.timer"
            android:label="@string/timer_name" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name=".slippery.slipperyActivity"
            android:screenOrientation="landscape"
            android:label="@string/slippery_name" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name=".internet.Internet"
            android:label="@string/internet_name" >
            <intent-filter>
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
